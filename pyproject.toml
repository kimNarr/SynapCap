[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "basic"
exclude = [".venv", "build", "dist", "artifacts"]

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [".venv", "build", "dist", "artifacts"]
