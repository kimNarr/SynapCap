# 변경 기록

이 프로젝트는 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르며 버전은 [Semantic Versioning](https://semver.org/lang/ko/)으로 관리합니다.

## [Unreleased]

### 추가

- 펼침·막대·트레이만 3단계 표시 모드와 모드별 화면 위치 저장 기능
- 사용 가능한 프로바이더 한도 중 가장 높은 사용률을 숫자와 단계 색으로 보여 주는 동적 트레이 아이콘
- 펼침·막대 두 모드의 창 제어를 `[트레이로] [모드 토글] [종료]` 같은 순서·아이콘으로 통일하고, 펼침 헤더에서도 곧장 트레이만 모드로 갈 수 있게 함 (모드 토글 아이콘: 펼침=가로 막대, 막대=둥근 사각)
- 트레이 아이콘에 표시할 수치를 "최고 사용률" · 특정 프로바이더 · "숫자 표시 안 함" 중에서 고르는 설정을 추가

### 변경

- 펼침과 막대 위젯을 작업표시줄·Dock을 제외한 사용 가능 화면 안에 배치해 셸과의 최상위 순서 경쟁과 깜빡임을 제거
- 트레이 아이콘 클릭 시 마지막으로 사용한 펼침 또는 막대 모드로 복원하고, Windows에서 트레이만 모드를 처음 선택하면 아이콘 고정 방법을 안내
- 집중 링 카드에서 `SESSION`/`WEEKLY` 라벨과 리셋 카운트다운을 흐린 보조 색·기본 굵기로 낮춰 링 안의 사용률 `%`가 확실한 헤드라인이 되도록 조정
- 헤더의 버전 표기를 업데이트가 있을 때만 노출하고(평소엔 트레이 툴팁·설정 창에서 확인), 세 프로바이더 모두 링·탭 툴팁에 "어느 CLI가 기록한 값인지 + 마지막 조회 시각"을 일관되게 표시
- 사용량이 75% 이상인 집중 링 카드에 단계 색 테두리를 둘러(75–89%는 2px, 90% 이상은 3px) 색각과 무관하게 "주의할 카드"와 위험 단계를 구분할 수 있게 함
- 로고 마크 색을 앱 UI 강조색과 동일한 블루(`#89B4FA` / 라이트 `#1857C9`)로 통일하고 SVG·아이콘·홈페이지 자산을 재생성
- 홈페이지 위젯 미리보기를 현재 집중 링 디자인에 맞춰 4단계 사용량 색, 흐린 SESSION/WEEKLY·리셋 라벨, 검정 중심 프레임, 창 제어 구분선으로 갱신
- 프로바이더 칩 세 개를 모두 테마 틴트 배경으로 통일해 Gemini만 흰 박스로 튀던 문제를 없애고, Claude 아이콘을 "Claude Code" 격자에서 공식 Claude 마크로 교체
- 트레이 아이콘의 칩이 아이콘 상자를 거의 꽉 채우도록 여백을 줄이고 자릿수별 숫자 크기를 키워 작은 트레이에서도 잘 보이게 함
- 설정창 하단을 `취소` / `저장` 2개로 정리(`적용` 버튼 제거) — 테마·표시 모드·트레이 표시는 고르는 즉시 미리보기, `저장`이 전체 반영, `취소`가 되돌리기
- 로고 게이지 트랙을 270° 호에서 완전한 원으로 닫고 트랙 색 대비를 올려, 작은 크기에서 로딩 스피너로 오인되던 문제를 없앰 (SVG·아이콘·홈페이지 자산 재생성)
- 인앱 로고·워드마크·프로바이더 칩을 화면 배율(HiDPI)에 맞춰 렌더해, 125% 이상 배율에서 흐리게 뭉개지던 문제를 수정
- 펼침 헤더 로고를 20→26px로 키우고 `S` 획을 굵게 해, 작은 크기에서 링만 보이던(스피너 같던) 문제를 해결
- 테마를 전환해도 헤더 로고가 다시 칠해지지 않아 다크에서 `S`가 사라지던 문제를 수정
- 컴팩트 막대 로고를 17→22px로 키움 (헤더와 같은 굵은 `S`)
- 첫 실행·업데이트 직후에는 위젯을 화면 중앙에 배치하고, 이후 실행부터는 마지막 위치·모드를 복원

### 수정

- 막대의 펼치기 버튼을 누르면 저장된 과거 좌표로 이동하던 동작을 현재 막대의 화면 모서리를 기준으로 펼치도록 수정

## [0.2.6] - 2026-09-03

### 수정

- Windows에서 작업표시줄과 겹쳐 둔 고정 위젯이 반복적인 작업표시줄 클릭 후 뒤로 가려지는 문제를 네이티브 최상단 순서 재확인으로 보정

## [0.2.5] - 2026-09-02

### 수정

- 펼침 위젯 고정 폭을 384px로 넓히고 탭 글꼴 크기를 픽셀 기준으로 고정해 모든 프로바이더의 `100 / 100%` 요약이 잘리지 않도록 보정

## [0.2.3] - 2026-09-02

### 수정

- Windows에서 작업표시줄로 다른 앱을 선택한 뒤에도 ‘항상 위에 고정’ 위젯이 뒤로 밀려 보이지 않던 문제를 보정

## [0.2.2] - 2026-09-02

### 개선

- 펼침 위젯 헤더에서 가로 워드마크 왼쪽에 앱 게이지 심볼을 함께 표시해 컴팩트 막대·트레이 아이콘과 브랜드 연결을 강화

## [0.2.1] - 2026-09-02

### 개선

- 가로 링 보기의 프로바이더 탭을 모델명과 사용량 요약의 크기가 다른 전용 카드로 바꾸고, 링 카드는 `SESSION/WEEKLY`·사용량·리셋 시간을 중앙 정렬로 배치
- 링 내부의 `SESSION/WEEKLY`와 `5h/7d` 기간 표식을 없애고, 카드 상단 라벨과 사용량·리셋 시간만 남겨 읽는 흐름을 단순화
- 컴팩트 막대의 두 사용량을 양쪽 여백을 둔 `87 / 61%`로 표기하고, 카드·테두리 없이 아이콘과 수치를 평면 그룹으로 정리
- 펼친 사용량 화면을 링 그래프 하나로 통일하고 막대·세그먼트·숫자 모드 및 그래프 모양 선택기를 제거했으며, 기존 설정값은 링으로 자동 이관
- 컴팩트 막대는 모델명 대신 아이콘과 `5h·7d` 수치만 남기고, 보통 상태는 절제된 굵기·위험 상태만 강조해 짧고 명확하게 개선
- 컴팩트 막대의 앱 로고 뒤와 창 제어 앞 세로 구분선을 제거해 시각적 복잡도를 축소
- 링 가로 배치를 모든 프로바이더 카드 나열 방식에서 상단 프로바이더 선택과 선택 모델의 대형 `5h`·`7d` 듀얼 링을 보여주는 집중 보기로 개편하고, 하단 갱신 문구는 새로고침 버튼 툴팁으로 이동
- 펼침 위젯을 336px 고정 폭과 가로 한 모델 집중 링으로 고정하고, 크기·링 배치·작업표시줄 회피 설정을 제거
- 화면 위치 보정 기준을 실제 화면 전체로 바꿔 작업표시줄·Dock과 겹칠 수 있지만 화면 밖으로 벗어나지 않도록 조정

### 수정

- 라이트 테마에서 기간·초기화 시각 툴팁이 어두운 배경과 어두운 글자로 표시되던 문제
- 툴팁이 대상 버튼에서 과도하게 떨어져 표시되던 Windows 위치 여백

## [0.2.0] - 2026-09-01

### 추가

- 다크·라이트·시스템 자동 테마와 OS 색상 모드 변경 시 실시간 전환 기능
- 링 그래프에서 프로바이더 카드를 세로 또는 가로로 배치하는 선택 옵션과 좁은 화면 자동 세로 폴백
- 설정을 저장하기 전에 현재 위젯 화면으로 바로 적용해 보는 "적용" 버튼
- 위젯을 화면 하단 작업 표시줄 바로 위에 고정하는 선택 옵션

### 개선

- 컴팩트 막대에서 프로바이더 간격·구분선 대비·세로 여백을 늘리고 앱 로고 뒤에도 구분선을 추가했으며, 기본 수치는 가볍게 하고 경고·위험만 굵게 표시
- 세그먼트 그래프를 짧은 고정 폭으로 정리하고 리셋 시간 열을 고정해 모든 모델·기간의 그래프 끝점과 수치 열 정렬 통일
- 라이트 테마에서 본문 WCAG AA 대비와 로고 가독성을 확보하도록 Catppuccin Latte 색상을 보정하고 라이트 전용 로고 색상 적용
- 모든 펼침 그래프 보기를 `기간 배지 · 그래프 · 리셋 · 사용량` 한 행 구조로 압축해 숫자 보기와 가까운 세로 높이로 정리
- 컴팩트 막대의 기본 글자를 한 단계 작고 덜 굵게 조정하고, 앱 로고·프로바이더 그룹 사이에는 구분되는 여백을 확보
- 컴팩트 막대에서 프로바이더 사이에 얇은 세로 구분선을 넣고, 두 사용량 창은 `76/44%`처럼 `%`를 한 번만 붙이며, 값 폭을 최대 자릿수 기준으로 예약해 숫자가 바뀔 때 막대가 출렁이지 않도록 정리
- 환경설정 "그래프 모양"을 4종(막대·세그먼트·링·숫자만) 미리보기 타일로 바꾸고, 의미가 불명확하던 위젯 헤더의 그래프 전환 버튼을 제거해 헤더를 아이콘 4개로 정리
- 사용량 색을 4단계(여유·주목·경고·위험)로 재조정하고, 60% 미만은 무채색으로 죽여 낮은 값이 실제로 봐야 할 값과 경쟁하지 않도록 함. 경고 임계 60→75%, 위험(`▲`) 임계 80→90%로 상향
- 펼침 화면 첫 카드에 `리셋까지 · 사용률` 컬럼 레전드를 한 줄 추가하고, 프레임 하단에 `N분 전 갱신` 갱신 시각 캡션을 상시 노출해 두 숫자의 의미와 데이터 신선도를 바로 알 수 있게 함
- `5h`/`7d` 기간 표식을 테두리 있는 배지에서 흐린 텍스트 라벨로 바꿔 버튼처럼 보이지 않게 하고, 헤더·컴팩트 바에서 앱 동작(새로고침·설정)과 창 제어(최소화·닫기) 사이에 세로 구분선을 넣어 닫기 오클릭을 줄임
- CLI가 설치되지 않은 프로바이더를 빨간 "설치 필요" 경고 대신 회색 "미설치" 상태로 표시하고 카드 이름도 흐리게 처리해, 안 쓰는 서비스가 오류처럼 보이지 않게 함 (로그인 필요·시간 초과 등 조치 가능한 상태는 그대로 경고색 유지)
- 기본 360px의 작게·기본·크게 전체 위젯 크기 프리셋을 추가하고, 기존 펼침·막대 글꼴 세부 옵션은 자동 이관 후 정리
- 프로바이더 설정을 한 줄 헤더형 카드로 정리하고 Codex·Gemini·Claude가 모두 등록되면 Add 버튼 비활성화
- 위젯·설정·트레이의 바탕을 검정 중심 팔레트로 조정하고 리셋 시간 표기를 `3h 25m`·`6d 16h`로 축약
- 펼침 막대 뷰의 사용량 수치를 막대 밖 오른쪽 고정 열로 분리해 낮은 사용률에서도 또렷하게 표시하고 행 정렬을 통일
- 사용량 막대를 직접 렌더링해 1%처럼 낮은 값도 최소 3px 채움으로 식별하고, 경고·위험 수치에는 굵기와 위험 표식을 더해 색 외 신호를 제공
- 펼침 막대 뷰의 리셋 시간을 보조 색·작은 글꼴·고정 폭으로 정리해 사용량 수치와 막대 시작 위치를 또렷하게 구분
- 5시간·주간 구분 표식을 `5h`·`7d` 기간 배지로 바꾸고 `CLI 기준`과 같은 계열의 블루 아웃라인으로 정리
- 막대·링 뷰의 리셋 표시를 `2시간 30분` 형식으로 통일하고 링 뷰에서 잘리던 문구 보정
- 환경설정 스크롤바와 피드백 카드 색을 새 어두운 팔레트에 맞춤
- Gemini 프로바이더 아이콘을 흰 바탕 + 브랜드 블루로 변경해 어두운 위젯에서 또렷하게 표시
- 디자인 언어와 UI 개선 backlog를 `docs/DESIGN.md`로 문서화

### 수정

- 컴팩트 막대의 마지막 혼합색 사용량 값이 실제 글자 폭보다 좁게 계산되어 우측이 잘리는 문제
- 하단 고정을 켠 상태에서 위젯을 세로로 옮기면 즉시 아래로 되돌아가 위치를 조정할 수 없던 문제
- 고정 폭 리셋 열에서 `초기화 확인 중`·`리셋 시각 미상` 같은 상태 문구가 잘리던 문제 — `확인 중`·`미상`으로 짧게 표시하고 전체 내용은 툴팁에 유지
- 프로바이더 카드 여백을 다시 잡아 이름이 자기 행에 붙고, 카드 사이 간격이 카드 내부보다 넓어 각 프로바이더가 한 덩어리로 읽히도록 조정
- 60~79% 경고 색을 파스텔 옐로에서 peach로 바꿔 어두운 바탕에서 또렷하게 표시
- 새 게이지 로고와 워드마크를 SVG로 앱·트레이·설치 아이콘에 반영하고 빌드 시 아이콘을 자동 생성
- 다운로드 홈페이지와 README의 로고·워드마크를 새 게이지 디자인으로 교체
- `CLI 기준` 배지를 아웃라인 칩으로 낮추고, 내부 프레임 테두리를 없애고, 버전 배지 대비를 살짝 올려 헤더·카드 정돈
- 컴팩트 바에서 5시간·주간 사용률의 경고 단계가 다르면 각 수치를 개별 색으로 표시
- 링 그래프를 키우고 안쪽 `%` 글꼴을 확대해 가독성 개선
- 첫 조회 전 카드에 스켈레톤 행을 표시해 데이터가 도착할 때 위젯 높이가 튀지 않도록 개선
- 사용량 그래프를 막대·세그먼트·링·숫자만 4종으로 늘리고 환경설정에서 선택. 모든 종류가 동일한 타일(창 이름·리셋 줄 고정 + 그래프 줄 교체)을 써서 전환 시 정렬이 흔들리지 않음

## [0.1.17] - 2026-08-28

### 수정

- 컴팩트 막대에서 펼침으로 돌아온 뒤 카드 컨테이너가 콘텐츠 폭으로 축소되어 오른쪽에 빈 공간이 남는 문제

## [0.1.16] - 2026-08-27

### 추가

- 프로바이더 상태 배지를 눌러 CLI 감지 경로, 최근 조회 시각, 설치·로그인 안내를 확인하고 토큰이 포함되지 않은 진단 보고서를 복사하는 기능
- 선택한 사용량 임계치에 도달하면 한도별로 한 번만 알리고, 사용량이 낮아졌다가 다시 상승할 때 재알림하는 선택형 알림
- 중복 실행 시 새 프로세스를 종료하고 이미 실행 중인 위젯을 앞으로 표시하는 단일 인스턴스 동작
- 업데이트 후 최초 한 번 해당 버전의 GitHub Release 안내를 표시하는 새 버전 안내

### 개선

- Small·Medium·Large 크기 옵션을 제거하고 글꼴 크기와 프로바이더 이름에 따라 300~480px 범위에서 펼침 폭을 자동 계산
- 컴팩트·막대·링 전환 시 창과 내부 프레임 크기, 가장자리 기준점을 함께 복원하도록 레이아웃 전환 구조 통합
- 사용량 툴팁에 마지막 조회 시점과 오래된 데이터의 새로고침 권장 상태 표시

### 수정

- 기존 Large 설정을 사용하던 위젯에서 막대·링 그래프 전환 또는 컴팩트 막대 왕복 후 내부 프레임 폭이 창 폭과 달라져 투명 여백이나 잘림이 생기는 문제

## [0.1.15] - 2026-08-27

### 수정

- Windows 업데이트 후 동일한 실행 파일 경로의 아이콘 캐시 때문에 바탕화면·시작 메뉴 바로가기에 이전 로고가 남는 문제
- 컴팩트 막대의 아이콘과 사용량 수치 사이에서 툴팁이 반복해서 사라졌다 나타나고 화면 아래쪽에서 막대와 겹치는 문제

### 개선

- 메인 위젯과 환경설정 외곽선을 2px로 선명하게 조정하고 화면·카드·입력·선택·버튼·막대의 모서리 곡률을 4~6px 체계로 통일

## [0.1.14] - 2026-08-27

### 개선

- 새 SynapCap 심볼과 워드마크를 앱, 트레이, 설치 아이콘, 홈페이지와 README에 일관되게 적용
- 작은 트레이 아이콘에서도 로고가 치우치거나 잘리지 않도록 투명 여백을 보정하고 Windows·macOS 앱 번들에 원본 자산 포함

## [0.1.13] - 2026-08-27

### 수정

- Large 위젯을 컴팩트 막대로 접었다가 펼칠 때 비동기 레이아웃 계산으로 폭이 Medium 크기로 축소되던 문제

### 추가

- 트레이 메뉴에서 즉시 업데이트를 확인하거나 SynapCap을 재시작하는 기능
- 시작 시 확인 이후에도 앱 실행 중 6시간마다 새 버전을 확인하는 주기 검사

## [0.1.12] - 2026-08-26

### 수정

- 한도 초기화 직후 캐시에 남은 과거 리셋 시각이 다음 해로 계산되어 `364일 후`처럼 표시되던 문제

## [0.1.11] - 2026-08-26

### 개선

- Codex App Server가 제공하는 5시간·주간 사용량을 모두 표시하고 프로바이더 설정에서 각각 표시 여부를 선택
- Claude 카드에 `CLI 기준` 배지와 마지막 조회 시각을 표시하고 Claude 화면과 일시적인 동기화 차이가 생길 수 있음을 안내

## [0.1.10] - 2026-08-12

### 개선

- 펼침 위젯과 컴팩트 막대의 글꼴 크기·굵기를 각각 설정하고 크기에 따라 폭·간격·진행 막대·링을 반응형으로 조절
- 기본 글꼴 크기를 펼침 13px, 컴팩트 12px로 확대하고 기존 굵기 설정을 새 펼침 설정으로 자동 이전

### 수정

- 첫 로딩 완료 후 컴팩트 막대가 스피너 폭에 고정되어 사용량 텍스트와 우측 영역이 잘리던 현상

## [0.1.9] - 2026-08-12

### 개선

- 위젯 위치에 따라 접기·펼치기 방향을 자동 선택하고 화면 가장자리 근처에서 자석처럼 스냅

### 추가

- 트레이 메뉴와 환경설정의 피드백 탭에서 버그·기능 제안·기타 의견용 GitHub Issue 양식 열기

## [0.1.8] - 2026-08-12

### 수정

- macOS에서 다른 앱이나 창을 클릭하면 트레이 위젯이 숨겨지던 도구 창 비활성화 동작

## [0.1.7] - 2026-08-12

### 추가

- 최초 조회와 수동 새로고침 중 프로바이더별 회전형 로딩 애니메이션 표시
- 컴팩트 모드에서 설정한 5시간·현재 세션 및 주간 사용량을 `15%/50%` 형식으로 함께 표시

### 변경

- 전체 위젯 폭은 크기 프리셋에 따라 최소 300px을 사용하고, 컴팩트 막대는 프로바이더 수와 실제 콘텐츠 너비에 맞춰 자동 조절
- 효용이 낮고 컴팩트 모드 복원과 충돌하던 수동 `위젯 세부 너비` 설정 제거
- 단일·복수 컴팩트 사용량의 글꼴 크기와 글꼴 계열을 통일

### 수정

- 컴팩트 막대를 펼칠 때 오른쪽 아래 기준 위치가 유지되지 않아 창이 작업 표시줄 쪽으로 내려가던 문제
- 복수 사용량 표기가 컴팩트 막대에서 잘리던 문제
- macOS에서 체크박스 체크 표시가 보이지 않거나 환경설정 컨트롤 모양이 달라질 수 있던 문제
- 프로바이더 삭제 버튼에 마우스를 올리면 아이콘과 배경색이 겹쳐 아이콘이 사라져 보이던 문제

## [0.1.6] - 2026-08-11

### 수정

- 메뉴 막대 전용 설정을 적용한 뒤 macOS 앱 번들을 다시 임시 서명하고 검증하도록 패키징 순서를 수정
- v0.1.5 배포 후보의 원클릭 업데이트, 컴팩트 보기 및 UI 개선 사항을 정상 설치 파일로 제공

## [0.1.5] - 2026-08-11

### 추가

- 공식 GitHub Release의 운영체제별 설치 파일을 내려받고 SHA-256을 검증하는 원클릭 업데이트
- Windows 업데이트의 UAC 승인 후 무인 설치와 SynapCap 자동 재실행
- macOS 업데이트의 아키텍처별 DMG 자동 선택·검증·열기
- Codex, Gemini, Claude 카드와 환경설정 목록에 각 서비스 브랜드 아이콘 적용
- 작업 표시줄로 보내지 않고 프로바이더별 사용률을 유지하는 가로형 컴팩트 모드 추가
- Windows 작업표시줄·Alt+Tab과 macOS Dock 대신 알림 영역·메뉴 막대에서 제어하는 트레이 전용 창 동작

### 수정

- GitHub API가 일시적으로 실패했을 때 다운로드 홈페이지가 이미 배포된 앱을 `첫 공개 버전 준비 중`으로 잘못 표시하던 안내 수정
- 홈페이지 위젯 미리보기의 버전 배지가 오래된 고정값 대신 최신 GitHub Release 태그를 표시하도록 수정
- 연결이 중간에 끊긴 업데이트 다운로드가 진행 상태에 멈추지 않도록 오류 복구와 작업 스레드 정리 강화
- 작은 너비에서 프로바이더 3개의 컴팩트 항목이 겹치지 않도록 컴팩트 막대 최소 너비 자동 보정
- 어두운 데스크톱에서도 컴팩트 막대가 구분되도록 배경·테두리 대비와 사용량 글자·아이콘 크기를 개선하고 `S` 로고의 시각적 세로 중심 보정
- macOS 환경설정에도 Windows와 같은 Fusion 컨트롤 스타일과 사용자 정의 창 디자인 적용
- Antigravity 사용량 조회 중 불필요한 백그라운드 자동 업데이터가 실행되어 Windows 콘솔 창이 잠깐 나타날 수 있던 문제

### 문서

- 홈페이지와 README에 원클릭 업데이트 동작, 무결성 검증 및 운영체제별 설치 차이 안내 추가
- 홈페이지와 README에 macOS 미서명 앱의 첫 실행 허용 절차와 `그래도 열기` 안내 추가

## [0.1.4] - 2026-08-11

### 추가

- Windows 및 macOS 릴리스 파일에 GitHub Actions 빌드 출처 증명 자동 생성
- README와 다운로드 홈페이지에 Windows Edge·SmartScreen 평판 경고와 무결성 확인 절차 추가

### 수정

- 환경설정에서 프로바이더를 제거한 뒤 저장할 때 위젯 높이가 즉시 줄지 않아 빈 테두리가 남던 문제

## [0.1.3] - 2026-08-11

### 수정

- Antigravity 사용량 조회가 macOS Keychain의 기존 CLI 로그인 상태를 그대로 사용하도록 인증 격리 방식 수정
- 사용자 홈은 유지하면서 MCP 실행에 사용되는 개발 도구 경로만 조회 프로세스에서 제외

## [0.1.2] - 2026-08-11

### 추가

- README와 다운로드 홈페이지에 오류 상태별 해결 방법, macOS CLI 설치·확인·로그인 안내 추가

### 수정

- macOS 사용량 조회가 보호 폴더와 네트워크 볼륨을 불필요하게 탐색하지 않도록 CLI 경로 검색 범위 제한
- Claude 및 Antigravity 사용량 조회를 사용자 MCP 설정과 분리해, 등록된 MCP 서버 실행과 macOS 앱 접근 권한 요청 방지
- Windows와 macOS 설정창이 동일한 사용자 정의 제목 표시줄과 창 디자인을 사용하도록 통일

## [0.1.1] - 2026-08-11

### 변경

- 정식 지원 운영체제와 동일하게 Windows·Ubuntu·macOS에서 CI 테스트 실행

### 수정

- macOS Finder에서 실행할 때 Homebrew·사용자 로컬·Node 버전 관리자의 CLI 경로를 찾지 못하던 문제
- macOS 설정 화면에서 긴 연결 설명과 한도 선택 문구가 잘릴 수 있던 문제
- 조회 실패 원인을 `설치 필요`, `로그인 필요`, `시간 초과`로 구분하지 않던 문제

## [0.1.0] - 2026-08-10

### 추가

- 설정에서 사용량 수치의 굵은 글꼴 표시를 켜고 끄는 옵션
- 프로바이더별 5시간·주간 한도 표시 여부 설정

### 변경

- 홈페이지 대표 다운로드 영역에서 Windows와 macOS 선택지를 함께 안내
- 위젯을 홈페이지와 통일된 프로바이더 카드형 디자인으로 개편
- 홈페이지와 위젯의 프로바이더 표기를 `Cx`, `G`, `Cl`로 통일
- 화면 설정 변경은 기존 사용량으로 즉시 다시 그리고 CLI 재조회는 생략
- 상단에 현재 버전과 클릭 가능한 업데이트 배지를 추가
- 전원·숨기기 버튼을 작업 표시줄 최소화와 완전 종료 버튼으로 교체

### 수정

- Windows 설정 화면에서 콤보박스 화살표가 보이지 않던 문제
- 숫자 입력의 증감 버튼이 나란히 표시되고 화살표가 보이지 않던 문제
- 프로바이더 목록 스크롤 영역에 밝은 배경이 노출될 수 있던 문제
- Claude 현재 세션이 0%이고 리셋 시각이 없을 때 5시간 한도가 숨겨지던 문제
- 링 그래프 안의 `사용` 문구와 퍼센트가 겹쳐 가독성이 떨어지던 문제
- 막대형 사용량 값의 중복 `사용` 문구를 제거하고 툴팁 표시 지연을 개선
- `×`, 작업 표시줄 닫기와 트레이 종료에 취소가 기본인 종료 확인창 추가
- 상단 로고·앱 이름·버전 배지의 세로 정렬 보정
- 리셋 툴팁을 `날짜 시간 초기화` 형식으로 간소화
- 즉시 툴팁을 마우스 위치가 아닌 대상 요소 바로 아래에 고정해 표시 위치를 안정화
- 홈페이지 위젯 미리보기의 기울기를 제거하고 수평으로 정렬
- 종료 확인창의 아이콘 영역까지 최소 너비가 적용되어 가로로 늘어나던 문제
- 홈페이지 보기 전환 안내를 위젯 우측 하단에 살짝 겹치는 말풍선 형태로 정렬
- 종료를 승인한 뒤 닫기 이벤트가 다시 발생해 확인창이 한 번 더 표시되던 문제
- Claude 사용량 조회를 안전 모드로 격리해 MCP·훅·플러그인·브라우저 창이 실행될 수 있던 경로 차단
- 상단 버전 배지를 로고와 같은 높이로 맞춰 세로 중심 정렬 개선
- 종료 확인창의 본문과 버튼 최소 폭을 줄여 더 컴팩트하게 표시
- 캐시된 동일 사용량은 기존 행 위젯을 재사용해 반복 갱신의 메모리 할당 감소
